package island.constants;

/**
 * This is an enum class, meaning it simply holds constant values.
 * We can use these as tokens to represent related things in Java.
 * 
 * In this case, the class contains Colors which a Cat can be.
 */
public enum Color {
    WHITE, ORANGE, BLACK, BROWN, GREY;
}
